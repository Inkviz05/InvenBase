<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="InvenBaseActionBar" parent="Widget.MaterialComponents.Toolbar">
        <item name="logo">@null</item>
        <item name="navigationIcon">@null</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="InvenBaseActionBarStyle" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="displayOptions">showTitle</item>
        <item name="logo">@null</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- Dark theme (same as light for this app) -->
    <style name="Theme.InvenBase" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="toolbarStyle">@style/InvenBaseActionBar</item>
        <item name="actionBarStyle">@style/InvenBaseActionBarStyle</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/secondary</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <!-- Material 3 color roles (required by ThemeOverlay.Material3.Button.TextButton etc.) -->
        <item name="colorSecondaryContainer">@color/secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/text_primary</item>
        <item name="colorOutline">@color/outline</item>
    </style>
</resources>
